<h1>注文情報確認</h1>
<table>
  <thead>
    <tr>
      <th>商品名</th>
      <th>単価(税込)</th>
      <th>数量</th>
      <th>小計</th>
    </tr>
  </thead>
  <tbody>
    <% @carts.each do |cart| %>
      <tr>
        <td><%= image_tag cart.item.image, size: "100x100" %><%= cart.item.name %></td>
        <td><%= (cart.item.price * 1.1).floor %></td>
        <td><%= cart.quantity %></td>
        <td><%= (cart.item.price * 1.1 * cart.quantity).floor %></td>
      </tr>
    <% end %>
  </tbody>
</table>
<table>
  <tr>
    <td>送料</td>
    <td><%= @order_fee %></td>
  </tr>
  <tr>
    <td>商品合計</td>
    <td><%= @total.round.to_s(:delimited) %></td>
  </tr>
  <tr>
    <td>請求金額</td>
    <td><%= (@order_fee + @total).round.to_s(:delimited) %></td>
  </tr>
</table>

支払方法
<%= @order.payment_i18n %>


<% if @selected_address_type == 'my_address' %>
  <p>自分の住所</p>
  <p>郵便番号: <%= @order_post_code %></p>
  <p>住所: <%= @order_address %></p>
  <p>宛名: <%= @order_name %></p>

<% elsif @selected_address_type == 'registered_address' %>
  <p>登録済みの住所</p>
  <p>郵便番号: <%= @order_post_code %></p>
  <p>住所: <%= @order_address %></p>
  <p>宛名: <%= @order_name %></p>

<% else @selected_address_type == 'new_address' %>
  <p>新しいお届け先</p>
  <p>郵便番号: <%= @order_post_code %></p>
  <p>住所: <%= @order_address %></p>
  <p>宛名: <%= @order_name %></p>
<% end %>
<%= form_with model: @order, url: orders_path, method: :post do |f| %>
<!--多分このhidden_fieldになんやらかんやら呼び出して保存するんやと思う。違ったらｽﾏｿ-->
  <!--%= f.hidden_field :payment %>-->
  <!--%= f.hidden_field :post_code %>-->
  <!--%= f.hidden_field :address %>-->
  <!--%= f.hidden_field :name %>-->
  <!--%= f.hidden_field :address_id %>-->
  <div>
    <%= f.submit "注文を確定する" %>
  </div>
<% end %>

<!--<div>%= link_to '注文を確定する', create_order_path, method: :post %></div>-->